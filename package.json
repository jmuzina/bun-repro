{
  "name": "ds25",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "repro/*",
    "repro/bin-scripts/**"
  ],
  "scripts": {
    "build": "lerna run build",
    "prepare": "bun run build",
    "clean": "lerna run clean && rm -rf node_modules .nx bun.lockb"
  },
  "devDependencies": {
    "lerna": "^8.1.9"
  },
  "dependencies": {}
}
